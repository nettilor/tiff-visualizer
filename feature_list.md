## Feature list
For all features ask the user before implementing if they will not be practical!
At each version bump, completed items are rolled up into CHANGELOG.md (grouped by area, per release); this file stays the working tracker.

Current released version: **1.5.0** (see CHANGELOG.md); completed items not yet in
the changelog are queued for **1.6.0**.

### To implement

### Proposed features (do not implement until moved to "To implement")
- [ ] Grid pages: when tiles would get too small, cap the grid at a chosen page size (e.g. 3×3) and flip pages with Page Up/Down; shared axes and playback keep driving all stacks — including off-page ones — so pages stay in sync.
- [ ] Shared contrast + global auto: a "Shared B&C" checkbox live-syncs any range edit to all stacks (like Shared channels, but for contrast), plus an "Auto (all)" that computes one range per channel from the pooled histogram of every open stack — fair brightness comparison across positions.
- [ ] Synced crosshair probe: with Shared view on, the cursor shows as a crosshair at the same image coordinates on every tile, with a compact readout of each stack's pixel values there — compare the same spot across all positions at once.
- [ ] Position bookmarks: save named (t, z) positions ("division @ t=34") and jump between them from a menu/keys; stored in sessions.
- [ ] Playback loop range: set in/out markers on the t (or z) bar and playback loops just that window — rewatch one event without scrubbing back each time.
- [ ] Per-tile time offset: like the lock, but softer — an offset-locked tile follows the shared t bar shifted by ±n frames, aligning events that start at different times in different positions.
- [ ] Split channels into tiles: explode the active stack into one tile per channel (same underlying data, z/t stay synced) for side-by-side channel comparison; one click merges it back.
- [ ] Condition tags: give each tile a short colored label (control / treated / …) shown in its header; sort or filter the grid by tag; optionally import a position→condition mapping from a simple CSV/text file.
- [ ] Spatial calibration: read pixel size from ImageJ metadata, µm scale bar, cursor position in µm
- [ ] Live ROI stats: the selection's per-channel mean/min/max/std shown in the pane and updating while scrubbing, without a Cmd+M per position
- [ ] Line profiles: a line tool with an intensity-profile plot along it
- [ ] Grid selections: mirror one selection onto every tile (with Shared view) and a Measure All that appends one row per tile
- [ ] Recent files menu

### To fix

### Completed
- [x] Bug-fix pass — files & data (from a full-app scan): Save As over a file that is still memory-mapped — after an earlier Save As, from a relative path, or with the same file open twice — truncated the original; every open stack mapping the target now moves into RAM first, and Save As waits for in-flight RAM preloads and background renders. Read-only TIFFs open (read-only maps). Stack paths are stored absolute but not symlink-resolved — a symlinked position keeps its own name in headers, montage labels and export names — while the folder lists, sessions and saves compare resolved paths, so relative and symlinked opens still match their folder box. A NaN pixel no longer blacks out float images (NaN-aware ranges, NaN renders black). RGB TIFFs open composite in red/green/blue. Fiji's single-channel min=/max= display range is read and written, 8- and 16-bit palette LUTs are used (colors follow pixel values), ImageJ "color" mode survives a save, single-channel stacks saved by the app reopen, and a flat float plane or float Reset keeps a range scaled to the data instead of +1
- [x] Bug-fix pass — sessions & selections: restored contrast shows immediately instead of the load-time one until you scrub; per-tile projection on/off and method survive a restore under MIP all; sessions store absolute paths and still open old relative ones; ellipses are measured and saved as the true ellipse (were a 24-sided polygon, ~1.2% area off, shrinking ~2 px per round trip); NaN pixels are left out of measurements like Fiji; replacing, right-click-removing or clearing a selection mid-drag can no longer crash the app or print errors (a pre-1.5.0 teardown-order bug)
- [x] Bug-fix pass — grid & shared axes: locked tiles keep their own bars through tiles joining/leaving, float without jumping to the shared position, and locks clear (without moving) when shared axes turn off; a projected tile stays drawn when the shared z is past its slice count; "★ only" clears when the last flagged tile closes or floats; stacks combined into an empty grid sync under Shared channels; playback keeps playing at its speed when tiles join or leave; turning Shared axes off leaves Minimalist; any drag returns the sort to Manual; Brightness sort ranks the displayed (projected) plane at the shared position, Sum like the mean it is shown as; a projected tile still steps the shared z for the grid; shared playback prefetches through the render pool instead of the UI thread (11 stacks: ~144 ms → ~1 ms UI time per tick, no dropped ticks)
- [x] Bug-fix pass — viewer: the pixel readout updates when the plane changes under a still cursor; with z collapsed to a projection, arrows/wheel/Space no longer move the hidden slice and the header drops its slice label; Save As and Projection keep the pane's Composite state, and a projection opens with the source's visible channels
- [x] Bug-fix pass — exports & windows: closing a floating window frees its stack (was kept in RAM and undercounted by the preload budget); Copy / Export PNG refuse a tile that is blank on screen, and single-tile movies and grid montage GIFs take the displayed (shared) position instead of a stale frame; Export Movie shows a cancelable progress and quantizes frames as it goes; every export reports success in the window you work in and a failed write in a dialog (grid montage included); Export Stack Montage remembers "collapse z" whatever the projection method (dialogs now remember combos by value) and no longer offers a collapse that leaves one tile; Open Folder, dropped folders and folder lists sort naturally (XY2 before XY10) and skip macOS ._ files
- [x] Bug-fix pass — B&C: histogram, Auto and Reset use the displayed plane (the projection when on, Sum in per-slice units); float images get enough decimals and a data-scaled minimum gap; the panel retargets when its stack closes, Apply to all never pushes a closed stack's range, and undo history no longer keeps closed stacks in RAM
- [x] Bug-fix pass — control window, startup & updates: a manual Check for Updates during the launch check still answers; files opened together from Finder/the Dock open as one batch; folder checkboxes match stacks opened via relative or symlinked paths; "darwin" release assets count as macOS while run-together names like TIFFVisualizerWin64.zip are still refused
- [x] t/z export ranges (Export Stack Montage gains "t range" and "z range" rows — `[from] to [to] of n`, combined with the every-nth steps, the z range greyed out while z collapses to a projection since that covers every slice as on screen; Export Movie and Export Grid Montage GIFs get a range for the animated axis, the other axis's row greyed out, the grid's counted over its longest stack so shorter tiles run into black "no image" frames; reversed boxes still read low→high; ranges are remembered only while narrowed, so a full range stays full on a longer stack, and clamp on a shorter one. Default file names say what is inside: movies `XY05_t3_z2-6.gif` instead of naming only the current frame, stack montages `XY05_montage_t2-4.png` / `_MIP` when narrowed or collapsed, grid GIFs `montage_t4-8.gif`; Export Movie on a stack with nothing to animate now says so instead of writing a one-frame GIF)
- [x] Montage exports label where every tile came from (Export Grid Montage: each tile's label now reads `XY05.tif  ·  c 1+2  z 5  t 12` — the displayed channels, z slice and t frame, with the projection (`z MIP`, `z AVG`, …) in place of the slice when it is on; only axes the stack has are named, GIF frames follow the movie's position, and frames past a stack's end read "no image". On by default, "Channel, z and t above tiles" checkbox to turn it off, remembered like the other options. When any tile's name + position would overflow its cell, every tile switches to name over position on two lines so the sheet — and every GIF frame — keeps one size; labels are clipped to their cell instead of running into the neighbor's. Export Stack Montage gains a title line above the sheet with the stack name, channels and the projection when z is collapsed (varying t/z stay labeled per tile), one per file in one-file-per-channel mode; "Stack name, channel and projection above the sheet" checkbox, on by default. Also fixed: a grid tile that is blank on screen (shared position past its end) exported its last frame instead of black)
- [x] Grid view lost the live pixel value (the bottom controls row of the grid window now mirrors the probe row of whichever tile the cursor is over — `XY05.tif:  x=120 y=80  value: 1834` — so Minimalist mode, which hides the per-tile probe row, still shows the value; the readout takes the slack between the mode checkboxes and the Sort/Grid combos so it costs no space, clips rather than widening the window, and clears when the cursor leaves the image or the tile leaves the grid; selection drawing status is mirrored the same way)
- [x] Selection tools + measure inside them (Fiji's rectangle, oval, polygon and freehand selections: one app-wide tool picked from the control window's tool row, Analyze → Selection Tool or the keys H/R/E/P/D — the Hand pans, the shape tools draw on a left drag; rectangle/ellipse drag out a whole-pixel box clipped to the image, freehand traces an outline, polygon takes a click per corner and closes on double-click or a click on the first corner. One selection per stack in Fiji yellow (pyqtgraph ROI items): drawing a new one replaces it, drag inside with a shape tool moves it (the hand always pans, even over a whole-image selection), handles resize it / move corners, click outside clears it, Esc abandons a shape mid-draw then clears, Cmd+A selects all, Cmd+Shift+A / right-click Remove ROI clear; the ViewBox routes left drags/clicks through roi.Selection first so the hand tool's pan/zoom is untouched. Cmd+M measures inside the selection — the shape is rasterized to a pixel mask exactly as drawn — with new ROI (shape + bounding box) and Area columns in the table and "whole image" rows when there is none; selections persist across z/t/channel changes and are saved in sessions)
- [x] Measure MFI with Cmd+M (Analyze → Measure, like Fiji: mean/min/max of the active stack's current channel over the whole image at the current z/t — the projected plane when the z projection is on — for floating windows and the active grid tile alike, shared axes included; each measurement appends a row (#, stack, c, z, t, mean, min, max) to a floating Measurements table (Analyze → Measurements Table) that pops up without stealing focus; Copy / Cmd+C copies the selected or all rows tab-separated with a header for spreadsheets, Save As… writes CSV, Delete drops selected rows, Clear / Analyze → Clear Measurements empties it; whole-image only for now, ROIs later)
- [x] Dock icon brings the main window to the front (clicking the app's dock icon raises the control window above the stack and grid windows and gives it focus, un-minimizing it first if needed — macOS only guarantees the *app* comes forward, so the control window used to stay buried under the stacks. Only real dock clicks do it: macOS answers one with a reopen that Qt reports as a second "app active" with no deactivation in between, so cmd-tab or clicking a stack window still leaves the window order alone)
- [x] Open all / Close all in dropped-folder lists (two buttons under each folder section's file list: Open all opens every stack in the folder that isn't open yet, Close all closes every pane showing one of them — including tiles inside the grid; each button greys out when it has nothing to do and the status bar reports "Opened/Closed n stacks". Both run as one batch: viewer.open_paths() loads the memory-mappable stacks and hands them to the grid in a single add_panes(), WorkspaceWindow.close_panes() detaches many tiles with one relayout, and the control window suspends its own refreshes for the duration — ~7x faster at 11 stacks and more beyond, since a per-stack relayout is O(n²). Open folder…, multi-file Open… and file drops go through the same batched path)
- [x] Z-projection type on the fly (the pane's projection checkbox is labeled with the live method — MIP / MIN / AVG / MED / SUM plus a ▾ caret — and right-clicking it offers Fiji's five methods (Max intensity, Min intensity, Average, Median, Sum slices); picking one also switches the projection on. The header reads "z AVG", PNG/GIF exports are named _AVG/_MED/…, the stack montage's collapse option follows the pane's method, and the workspace's "MIP all" gained the same right-click menu, applying the chosen method to every tile. Sum scales its display window by the slice count so it reads like the mean on screen while the pixel probe reports the true sums; the method is saved in sessions)
- [x] Export/projection dialogs remember their last-used options (Export Stack Montage, Export Grid Montage, Export Movie and Projection reopen exactly as last accepted — Cancel changes nothing; choices a stack doesn't offer are skipped and spin values clamp to its range; a t-less or z-less stack never overwrites the remembered step for the axis it lacks; the custom montage grid is kept from the last time Custom was actually used; a pane with MIP on still preselects the MIP export; a projection range is remembered only when it was narrowed, so a full range stays full on taller stacks; stored under dialogs/* in QSettings via settings.restore_widgets/save_widgets)
- [x] Stack montage grid layout (Layout combo in Export Stack Montage for a single varying axis — t, z, or t with z collapsed to MIP: Auto grid (near-square, the old default) / One row / One column / Custom columns × rows; the cols × rows boxes always show the effective grid and unlock in Custom, where editing one dimension grows the other only when the typed grid would drop tiles, so oversized grids simply leave black cells and the size estimate reports how many are empty; when both t and z vary the sheet stays t across × z down and the layout controls are disabled)
- [x] Update checking from GitHub (tiff_visualizer/updater.py, ported from pLayout's UpdateChecker: looks at nettilor/tiff-visualizer's releases/latest at most once a day, silent when up to date and silent offline; File > Check for Updates… — the app menu on macOS — always answers, including "didn't reach GitHub"; an offer shows the release notes excerpt with Download & Open / Not Now / Skip This Version, downloads the platform's asset into ~/Downloads with a cancelable progress dialog and opens it to drag into Applications; malformed tags and foreign-platform assets are never offered; auto-check toggle in Settings; policy functions covered by the regression suite without a network)
- [x] Number-key channel switching (1–9 on the active stack: in Composite each digit toggles that channel's visibility, otherwise it jumps the c bar to that channel; syncs with the pane's numbered boxes, the B&C panels and shared-channels mode; digits past the stack's channel count are left unhandled. Plain digits chosen over Cmd+1 — that's already View → Actual Size)
- [x] B&C channel radios could not switch channel in shared-axes grid mode (clicking "Ch 2" wrote to the pane's own hidden c bar, which `_sync_shared` immediately snapped back, so the only way to adjust another channel's contrast was dragging the shared slider; `set_channel` now drives the shared c bar like the arrow keys do, while locked tiles keep steering their own)
- [x] Single-stack montage (Image → Export Stack Montage…, Cmd+Alt+M: one PNG contact sheet with t across columns and z down rows, t/z labels framing the sheet; a single varying axis wraps into a near-square grid with corner labels; z can instead be collapsed to a max projection — preselected when the pane's MIP is on; every-nth t/z steps and full/half/quarter resolution with a live output-size estimate in the dialog; channels exported as displayed (visible channels) or one file per channel with _C1/_C2 suffixes; cancelable progress)
- [x] Solo / focus tile (Enter or double-click a tile's header fills the workspace with that tile — same position/zoom/contrast; Esc or Enter drops it back; hidden tiles skip rendering while soloed so scrubbing gets single-window fast; no new on-screen controls)
- [x] Tile flagging & triage (F flags/unflags the active stack with an amber ★ in its header; the "★ only" filter checkbox appears in the grid controls only once something is flagged — zero clutter before; unflagging the last tile auto-clears the filter; flags persist in sessions; View → Copy Flagged Names puts the list on the clipboard)
- [x] Tile sorting (Sort combo next to Grid: Manual / Name (natural order so XY2 < XY10, joining tiles slot in sorted) / Brightness (one-shot, brightest first by mean intensity of the visible channels at the current position — reselect to re-sort); dragging a tile returns to Manual; sessions now also preserve the grid order)
- [x] Contact-sheet export (Image → Export Grid Montage…, Cmd+Shift+M: the displayed tiles in grid order as one labeled montage — PNG at the current position or GIF over t/z with fps and full/half/quarter resolution options; per-tile contrast/channels/MIP honored, differing sizes letterboxed, out-of-range frames black like shared-axes blanks, cancelable progress for GIFs; respects ★ only/solo so you export what you see)
- [x] main window opens as really long on startup wasting extra space (quitting while a folder list had the window stretched tall saved that height; startup now keeps the saved position and width but compacts the height to the natural button-grid size, since folder lists never exist at launch)
- [x] icon v3: every 2×2 group of the old 8×8 coalesced into one big square — a bold 3×3 grid, columns in Italian-flag order (green, white, red) fading downward like z-slices; dim outer ring dropped for fewer, larger shapes
- [x] shared c-bar channel checkboxes: when shared axes + shared channels are both on, the numbered visibility boxes appear next to the shared c bar (colored by LUT, controlling all stacks; two-way sync with B&C and pane boxes; hidden when shared channels is off since per-tile visibility would be ambiguous; also fixed a desync where a pane joining the grid reset the boxes' displayed state)
- [x] permanent regression suite (tests/regression.py: 23 checks over the full feature matrix — model layer, navigation, grid modes incl. minimalist, async rendering, B&C, sessions, folder lists, preload, export; run before releases); also fixed sessions not capturing the Minimalist and MIP-all grid modes
- [x] minimalist mode in grid view ("Minimalist" checkbox in the grid controls: forces shared axes on, strips every per-tile button and control (float/close/lock/B&C, Composite/MIP, probe), leaves only name + info above each image, zero spacing and 2px dividers; joining tiles adopt it; disabling or floating a tile restores full chrome; persists like the other grid modes)
- [x] channel slider takes up space but there will never be more than a couple channels anyways so let's re-use some of that horizontal space. Shrink the bar a bit and to the right let's have checkboxes to turn channels on and off like in the B&C window. Each checkbox just says the number of the channel it represents and can be toggled (numbered boxes colored by each channel's LUT sit to the right of the shrunken c bar; two-way sync with the B&C panels and shared-channels mode; hidden with the bars in shared-axes mode, back on locked tiles)
- [x] folder list stretches with the window: removed the 280px height cap so dragging the control window taller gives the file list all the vertical space (894px list in a 1200px window); short windows scroll as before
- [x] when dropping a folder onto the main window the main window will expand with a new section below with the name of the folder and an x next to it to close it and then a list of each file in the folder with a checkbox next to it. Clicking the checkbox opens the files and unclicking closes them. Clicking the x next to the folder restores main window to normal. Folder name to the left has folder icon (multiple folders can be dropped, each gets its own section; long lists scroll within the section; checkboxes stay in sync when stacks are opened/closed elsewhere — including closing a tile in the grid; duplicate drops ignored; plain file drops still work)
- [x] Workspace no longer grows beyond the screen with many tiles (tiles had a 240×220 minimum that forced the window size; tiled panes now accept 170×150, and past that the grid scrolls inside the window instead of resizing it — small grids fill the window exactly as before, floating windows keep the larger minimum)
- [x] Parallel tile rendering (shared-axes ticks and grid playback render in a thread pool across cores instead of serially on the UI thread; main-thread block per tick with 48 stacks: ~110 ms → ~23 ms, so buttons stay responsive during playback; stale in-flight frames are dropped when scrubbing fast; single-pane navigation remains synchronous)
- [x] Text size control in Settings (9–24 pt spinner applies app-wide instantly — labels, bars, menus all scale; Default button returns to the system size; persists across launches; fixed-width labels loosened so nothing clips at large sizes)
- [x] Spacebar toggles time playback (shared t bar when the grid is in shared-axes mode; a locked tile plays its own bar; stacks with no time axis fall back to playing z)
- [x] RAM preloading with budget (Settings window via Cmd+, / control-window button: "Preload stacks into RAM when they fit" + GB budget spinner + live usage readout; stacks copy into RAM in background threads within the budget, over-budget stacks stay memory-mapped; raising the budget retro-preloads open stacks; render cache also grown to hold a full t-loop — fixes choppy first-pass timelapse playback)
- [x] in grid view add option to max project all panes in the grid ("MIP all" checkbox in the grid controls row; toggles every tile's MIP, stacks joining while on adopt it, individual tiles can still deviate afterwards)
- [x] remove B&C window snapping to image when dragged, it's too clumsy (drag-to-fuse deleted; the per-tile B&C button remains the way to attach a fused contrast panel)
- [x] New app icon: dark sibling of the plate-layout app's icon — same squircle/grid/column-color language, but rounded image tiles (the tiled workspace) instead of well dots, in the app's channel colors (red/green/gray) with rows fading like descending z-slices
- [x] Shared view: sync pan/zoom across grid tiles ("Shared view" checkbox links all tiles' viewboxes; zoom into a region on one and every tile shows the same region)
- [x] Playback: play/pause ▶ button on every z and t bar including the shared bars (right-click the button for 2–30 fps; wraps around; grid animates in lockstep with shared axes)
- [x] Open folder: File > Open Folder… (Cmd+Shift+O) opens every TIFF in a folder, also a control-window button
- [x] Session save/restore: File > Save/Open Session… (Cmd+Alt+S/O) stores open stacks, positions, contrast, channel state, MIP/locks, window geometries and grid arrangement as JSON; sessions auto-save on quit and Restore Last Session (Cmd+Alt+R) brings everything back; missing files reported
- [x] Export views: Image > Copy View (Cmd+C), Export View as PNG (Cmd+E), Export Movie GIF over T or Z with fps choice (Cmd+Shift+E) — all at full resolution with current contrast/channels
- [x] Live MIP toggle per pane (MIP checkbox next to Composite: max projection over z while scrubbing t; z bar disabled while on; header shows "z MIP"; pixel probe reads projected values)
- [x] Rendered-plane cache + neighbor prefetch (16-entry LRU per pane keyed on position/channels/stride/contrast-version; adjacent z/t planes pre-rendered at idle)
- [x] Threaded loading for compressed TIFFs (uncompressed stays instant via memmap; compressed decode in a background thread with a status message, UI never freezes)
- [x] Persistent settings (last folder for all dialogs, B&C window geometry, control window position, shared axes/channels/view and grid arrangement survive restarts)
- [x] Keyboard cheatsheet on "?" (View menu and control-window button too)
- [x] Per-tile lock 🔓/🔒 to exempt a pane from shared axes (locked pane keeps its position and gets its own bars back while the rest scrub; unlocking re-adopts the shared position)
- [x] visual indicator for tile drag-rearranging (blue insertion bar on the hovered tile's left/right edge tracks the cursor live, showing exactly where the tile will land; hidden over the dragged tile itself; live-sliding was considered and rejected as tiles would shift under the cursor)
- [x] in split view the window title and details are great but not functional. Make that whole region of the bar draggable to rearrange the windows in grid mode. (drag any tile's header strip onto another tile: left half inserts before it, right half after; drag preview shows a thumbnail of the tile; header text selection is disabled only while tiled)
- [x] add a button to B&C window to apply current range to all open panes (changing ranges should work with cmd+z to undo if it does not already) (Apply to all button in both the floating B&C and fused docks applies the active channel's min/max to the same channel of every open stack; Cmd+Z / Image > Undo Contrast Change undoes any range edit — slider drags coalesce into one step, and one undo reverts a whole apply-to-all)
- [x] add alt+cmd+g to bring up a menu with checkboxes, one for each open pane, to select which ones to put in grid mode. This is an alternative for the user when they only want some panes in the combined windows veiw. The non selected panes should remain individual (View > Combine Selected…, Alt+Cmd+G; checkboxes pre-set to current grid membership, Select all/none buttons; unchecked grid panes float back out with their remembered window positions; empty selection dissolves the grid)
- [x] Dock icon drops: the macOS bundle now declares TIFF document types in Info.plist (built via packaging/tiffviz_macos.spec), so dropping files on the Dock icon and Finder "Open With" work; routed through the existing Qt FileOpen handling
- [x] add the ability to drop one or multiple files on the main app window to open files (drops accepted on the control window and the workspace grid; non-TIFF files in the drop are ignored; stacks dropped on the workspace join the grid; the bundled macOS app also handles `open -a "TIFF Visualizer" file.tif`)
- [x] Version 1.0 shipped as a self-contained app: macOS .app built via packaging/build_macos.sh (PyInstaller, ~130 MB, includes Python+Qt+deps); packaging/build_windows.bat builds the Windows equivalent on any Windows machine with Python; flat stacked-slices icon in channel colors generated by packaging/make_icon.py
- [x] B&C floating window is too long, make it square by default using the current default height as the width too (sizes itself square on first show from its content height; still freely resizable afterwards)
- [x] zooming out is capped at the size of the window. It does not make sense to have a tiny image in a big window. Resizing the hole window by dragging the corners is allowed and already works. When using cmd+- or pinch to zoom out the image should not be smaller than the size of the window it originally opens to, it should snap to that (any zoom-out past fit-to-view snaps back to fit; applies to Cmd+-, pinch and Cmd+wheel; fit recomputes from the live window/tile size so resizing still works)
- [x] pinch to zoom does not hone in to where the mouse is pointing (the gesture event's own position is unreliable on macOS; now anchored on the actual mouse cursor position, verified to hold the pointed image pixel fixed through repeated zooms)
- [x] Pinch to zoom to enlarge images in the panes (trackpad pinch zooms centered on the cursor; works alongside Cmd+wheel and the View menu zooms)
- [x] Option to sync "composite" and selected channels to show (basically sync what channels are on) with checkbox next to the "shared axes" checkbox. Called "shared channels" (any pane's Composite toggle or channel visibility change propagates to all panes; stacks with fewer channels ignore extra indices; new panes joining the grid adopt the shared state)
- [x] B&C window that can fuse to the left or right side of a pane. Each pane can have its own individual B&C window that can be dragged to snap to right or left side. This builds on top of the uniquely shared B&C window. (Each pane's header has a B&C button that toggles an embedded side panel; dragging the shared B&C window onto a pane's left/right edge fuses it there; ⇄ swaps side, ✕ closes; shared panel still follows focus and stays in sync.)
- [x] Performance at scale (found in 48-stack stress test): combining into the grid was O(n²) (30s for 48 stacks, now 1.4s); scrubbing shared axes re-rendered all panes at full resolution (1.5s/step for 48 stacks, now 0.1s via adaptive downsampling that returns to full resolution when zoomed in).
- [x] Checkboxes in dark theme are also black so it's hard to click them. Make the inside a more visible gray for the user to see (also styled radio buttons; checked state is accent blue with white checkmark)
